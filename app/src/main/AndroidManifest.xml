<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.c301t19.cs.ualberta.seekaride" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar" >
        <!--<receiver -->
        <!--    android:name="core.NetworkChangeReceiver"-->
        <!--    android:label="NetworkChangeReceiver" >-->
        <!--    <intent-filter>-->
        <!--        <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />-->
                <!--<action android:name="android.net.wifi.WIFI_STATE_CHANGED" />-->
        <!--    </intent-filter>-->
        <!--</receiver>-->
        <service android:name=".activities.MyService" ></service>
        <receiver android:name=".activities.PollServerBroadcastReceiver" ></receiver>
        <activity android:name=".activities.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.RiderActivity" android:launchMode="singleTask"/>
        <activity android:name=".activities.DriverActivity" android:launchMode="singleTask"/>
        <activity android:name=".activities.AddRequestActivity" android:launchMode="singleTask"/>
        <activity android:name=".activities.SearchRequestsActivity" android:launchMode="singleTask"/>
        <activity
            android:name=".activities.NewAccountActivity"
            android:label="@string/title_activity_new_account" />
        <activity
            android:name=".activities.ViewProfileActivity"
            android:label="@string/title_activity_view_profile" />
        <activity
            android:name=".activities.EditAccountActivity"
            android:label="@string/title_activity_edit_account" />
        <activity
            android:name=".activities.EditRequestActivity" android:launchMode="singleTask"
            android:label="@string/title_activity_edit_request" />
        <activity
            android:name=".AcceptRiderActivity"
            android:label="@string/title_activity_accept_rider" />
        <activity
            android:name=".activities.AcceptDriverActivity"
            android:label="@string/title_activity_accept_driver" />
        <activity
            android:name=".activities.SearchResultsActivity"
            android:label="@string/title_activity_search_results" />
        <activity
            android:name=".activities.ViewOfferActivity"
            android:label="@string/title_activity_view_offer" />
        <activity
            android:name=".activities.ReviewOfferActivity"
            android:label="@string/title_activity_review_offer" />
        <activity
            android:name=".activities.MapActivity"
            android:label="@string/title_activity_map" />
        <activity
            android:name=".activities.RCompleteActivity"
            android:label="@string/title_activity_rcomplete" />
        <activity android:name=".activities.TestingActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.ChooseLocationActivity"></activity>
    </application>

</manifest>
